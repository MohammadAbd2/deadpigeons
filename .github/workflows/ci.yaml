name: CI - Build and Push to Main

on:
  push:
    branches:
      - master

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # 2. Setup .NET
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 10.0.x

      # 3. Restore dependencies
      - name: Restore backend dependencies
        run: dotnet restore server/api/api.csproj

      # 4. Build backend
      - name: Build backend
        run: dotnet build server/api/api.csproj --configuration Release --no-restore

      # 5. Publish backend
      - name: Publish backend
        run: dotnet publish server/api/api.csproj -c Release -o ./publish

      # 6. Upload backend artifact
      - name: Upload backend artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend
          path: ./publish

      # 7. Force push to main
      - name: Force push to main
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git fetch origin main --unshallow || true
          git checkout main
          git reset --hard $GITHUB_SHA
          git push https://${PAT_TOKEN}@github.com/${{ github.repository }} main --force

  build-frontend:
    runs-on: ubuntu-latest
    needs: build-backend
    steps:
      # 1. Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # 2. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 3. Install frontend dependencies
      - name: Install frontend dependencies
        run: |
          cd client
          npm install

      # 4. Build frontend
      - name: Build frontend
        run: |
          cd client
          npm run build

      # 5. Upload frontend artifact
      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend
          path: client/dist
