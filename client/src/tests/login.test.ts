import { describe, it, expect } from "vitest";

describe("Login Page", () => {
    it("renders login form", () => {
        // placeholder
        expect(true).toBe(true);
    });

    it("should eventually validate username and password", () => {
        expect(true).toBe(true);
    });
});
